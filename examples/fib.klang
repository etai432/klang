fn fibonacci2(n) {
    if n <= 1 {
        return n;
    }
    return fibonacci2(n - 1) + fibonacci2(n - 2);
}
let x = std::time();
let fibIndex = 15;
print("fib of 10: {fibonacci2(fibIndex)}");
print("this took {std::time() - x} seconds to calculate");